import React from 'react';

/**
 * ChatAgent hiện đã được tích hợp vào khung chat chung (`AIChatbot`),
 * nên component này không còn hiển thị gì trên UI nữa để tránh trùng lặp
 * và che nội dung. Vẫn giữ file và export để không ảnh hưởng tới import hiện tại.
 */
const ChatAgent: React.FC = () => {
  return null;
};

export default ChatAgent;
